import 'package:flutter/material.dart';

class PersonalPayment extends StatefulWidget {
  const PersonalPayment({Key? key}) : super(key: key);

  @override
  State<PersonalPayment> createState() => _PersonalPaymentState();
}

class _PersonalPaymentState extends State<PersonalPayment> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
